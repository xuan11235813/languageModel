
class Candidate:
	def __init__(self, sourceSequence, targetSequence, value):

		# sourceSeq is the total source sequence
		# targetSeq is the candidate target sequence for now
		# value is a list, which include source num * 1 Q
		self.sourceSeq = sourceSequence
		self.targetSeq = targetSequence
		self.value = value

	def getCandidateSample(self):
		return sample 

	def breeding(self, newTargetList, value):
		return 0
